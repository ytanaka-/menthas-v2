//import assert from "assert"
import client from "../crawler/hatebu-client"

/*
describe("各メソッドの動作", () => {
  it("getEntryArray", () => {
    client.getBookmarkerURLList("atria", 0, (err, result) => {
      if (err) {
        console.log(err);
      }
      console.log(result);
    });
  });
});*/
